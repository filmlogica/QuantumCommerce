services:
  - type: web
    name: quantumcommerce-web
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend.app:app --bind 0.0.0.0:8000
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
